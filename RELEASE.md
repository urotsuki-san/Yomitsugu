# Yomitsugu 0.2.9 Preview

ローマ字を打つそばから、日本語に変える Windows IME です。英語を交えた文章も、モードを切り替えずに入力できます。

## インストール

1. [Windows x64用インストーラー](https://github.com/urotsuki-san/Yomitsugu/releases/download/v0.2.9-preview.1/Yomitsugu-0.2.9-preview-x64-setup.exe)をダウンロードします。
2. ファイルを開き、画面の案内に従います。管理者権限が必要です。
3. `Win` + `Space` で Yomitsugu を選びます。

設定画面は、通知領域の Yomitsugu アイコンを右クリックして開きます。アイコンが隠れている場合は `^` の一覧にあります。スタートメニューの「Yomitsugu → 設定と辞書」からも開けます。

## 入力と候補の選択

| 操作 | 動作 |
| --- | --- |
| ローマ字を入力 | 入力中の文章に変換候補を表示 |
| `Space` | 候補の選択を開始。続けて押すと次の候補へ |
| `↑` / `↓` | 候補を移動 |
| `Enter` | 選んだ候補を確定 |
| `Esc` | 候補の選択を取り消し、読みの編集に戻る |
| `Backspace` / `Delete` | 未確定の入力を削除 |
| `F6` / `F7` / `F8` | ひらがな / 全角カタカナ / 半角カタカナ |
| `F9` / `F10` | 全角英数 / 半角英数 |

`insuto-ru` は「インストール」、`aninsuto-ru` は「アンインストール」に変換します。`yajirushi` では矢印、`maru` では丸などの記号を選べます。`ltu` や `xtu` は小さい「っ」、文章中の `.` と `,` は「。」「、」になります。

## ユーザー辞書

設定画面の「ユーザー辞書を編集」を開き、読み・単語・品詞を入力して登録します。登録した単語は一覧から修正・削除できます。最後に「保存」を押すと、次の変換から反映されます。

「TSV辞書を取り込む」では、Google日本語入力やMozcから書き出したUTF-8の辞書を読み込めます。取り込みは**現在の辞書を置き換えます**。元の内容は `.bak` に保存されるので、戻すときはこのファイルを取り込んでください。

```text
しゃないつーる	社内ツール	名詞
よみつぐ	Yomitsugu	固有名詞
```

辞書は最大4 MiB・10万件、同じ読みには32候補まで登録できます。保存先は `%LOCALAPPDATA%\ImeMixed\user_dictionary.tsv` です。

## 候補の学習

確定した候補をPC内に記録し、同じ読みの次の変換で優先します。記録は最大2,048件です。ユーザー辞書への登録がある場合は、そちらを優先します。

設定画面の「候補の選択を学習する」で切り替えられます。「学習履歴を削除」を押すと、学習した順位を初期状態に戻せます。ユーザー辞書はそのまま残ります。

## 公開辞書の更新

「公開辞書を更新」を押すと、MozcとEDRDGの公開データを取得します。一般語・外来語・英語略語・記号を含む辞書です。同梱版があるため、オフラインでも変換できます。

入力文、ユーザー辞書、学習履歴は送信しません。更新した公開辞書はユーザー辞書とは別のファイルに保存されます。

## アプリの更新

「アプリを更新」を押すと、GitHubで公開されている最新版を確認します。新版があれば、ダウンロードするかを選べます。取得したファイルのサイズとSHA-256を確認してから、インストーラーが開きます。

更新後は、入力に使っていたアプリを開き直してください。切り替わらない場合は、Windowsからサインアウトして入り直します。ユーザー辞書と学習履歴は引き継がれます。

## アンインストール

Windowsの「設定 → アプリ → インストールされているアプリ」で Yomitsugu を選び、「アンインストール」を押します。ユーザー辞書と学習履歴は残るため、再インストール後も使えます。

## この版の変更

- インストール先で変換エンジンが終了する不具合を修正しました。
- 入力直後にEnterを押した際、候補が届く前の文字列を確定する問題を修正しました。
- 候補の学習、学習の切り替えと履歴の削除を追加しました。
- 設定画面と通知領域のメニューにアプリの更新を追加しました。

## 対応範囲

Windows x64向けの評価版です。文節の伸縮と確定後の再変換は開発中です。文脈による同音語の選択や、複数箇所のタイプミスには改善の余地があります。

詳しい入力例と測定結果は[変換品質の記録](https://github.com/urotsuki-san/Yomitsugu/blob/main/docs/conversion-quality.md)、ビルド手順は[開発ガイド](https://github.com/urotsuki-san/Yomitsugu/blob/main/docs/development.md)を参照してください。

## ライセンス

独自コードはMITです。辞書・モデル・同梱ライブラリーにはそれぞれのライセンスが適用されます。出典は `THIRD_PARTY.md`、ライセンス本文は `licenses` フォルダーにあります。
