# モード切替不要・英日混在AI IME 開発仕様案 v2
## Google日本語入力相当の使用感を優先

調査・改訂日: 2026-09-24  
版: v2。初版の入力体験仕様を本書で置換する。
状態: 調査に基づく設計提案。Windowsでのビルド、入力試験、性能測定は未実施。付属JSONLは試験の種であり、実測結果でも学習済みモデルでもない。

## 今回の要求変更と優先順位

ユーザー追加要件: **「使用感はグーグル日本語入力と同等にしてほしい。」**

Google日本語入力から、日英の手動切替とタイポを直す負担を取り除くことを目標にする。既存のSpace変換・候補送り、Enter確定、文節修正、文字種変換を別の操作へ置換しない。英日判定・補正・変換候補の生成は自動のままとし、ユーザーが通常のキー操作で介入できる。

初版からの重要な修正:

- 「Spaceによる変換開始を要求しない」は、自動変換もできるという意味に限定する。Spaceの変換機能を廃止しない。候補変更に独自キーを必須にしない。
- 自動変換のプレビューと、ユーザーが開始した候補選択を別状態として扱う。自動で候補を計算・表示しただけで、最初のSpaceを「次候補」へ進める操作に変えない。
- ユーザーが候補・文節・文字種を操作したら、同じ未確定入力に対するAIの自動上書きを抑止する。
- 文字未入力時やIMEが処理しない状態では既存のキー割当を尊重する。英語区間の空白入力だけを、無切替機能に必要な拡張として扱う。
- 句点の勝手な追加は既定オフ。タイポ補正と文末補完は別機能とする。初版の句点追加を含む目標例は、補完を有効にした個別テストとして残す。

これは使用感の実装目標であり、Google日本語入力との完全互換や同等性能の達成報告ではない。自動英日判定とライブ表示は意図的な差分であり、それ以外の操作差分を最小にする。

## 1. 目的と採用判断

英語とローマ字日本語を区別せず入力し、日本語部分だけを自然に変換する。通常の入力で英数／かなモードの切替を要求しない。変換は自動でも利用でき、Space変換・候補選択・Enter確定という従来操作も維持する。タイポも補正するが、入力者が意図した英語・識別子を勝手に書き換えない。

ユーザーの目標例:

- `Githubnoripojitoriwokousinnsitekudasai` → `Githubのリポジトリを更新してください。`
- `konnitiwa` → `こんにちは。`
- `kiyoiitennkidesune.` → `今日はいい天気ですね。`

1、2例目の句点追加は文末補完。3例目は単純なかな漢字変換だけでなく、誤打鍵修復や助詞補完を含む目標である。入力だけで意図が一意に定まるという前提を置かない。

採用する構成は「Mozc由来のキー処理・Windows TSF・候補UIを維持するMyIME系基盤 + 生の入力を保持する混在デコーダ + AzooKey/Zenzai」。Google日本語入力相当の使用感を、フォーク先選定の必須条件に追加する。Jev/OpenJevは必須依存にせず、比較実験用アダプタにとどめる。最初は文字n-gram・ローマ字規則・辞書による小さな判定器を使い、不足が実測されたときだけ専用の文字単位モデルを学習する。

これは完成品の採用決定ではない。候補基盤のビルド再現性、既知不具合、差分量、ライセンス、アプリ互換性を確認し、固定コミットで比較してからフォーク先を確定する。

## 2. 調査で確認できた事実

MyIMEはMozcのWindows入力UIとSwiftのAzooKeyエンジンを接続し、Zenzaiとタイポ候補を備える。現行文書ではCPUが既定で、Vulkanも選べる。ただし、そのままで今回の無切替入力が完成するわけではない。[S1]

MyIMEの変換APIはひらがなを受け取り、候補の対応長もひらがな文字数である。今回必要な生ローマ字の境界・原綴り・カーソル対応を、このAPIだけから復元する設計にしない。DLL初期化失敗時の既存フォールバックは変換を無効にするもので、完全な辞書変換継続と同一ではない。[S2]

Zenzai上流は左文脈に対応し、v3.2では右文脈も扱う。推論回数上限は通常1で十分とする説明がある。[S3] `zenz-v3.2-small` のQ5_K_M重みは73.9MBである。これは重みの大きさであり、アプリ全体のRAM使用量ではない。[S4]

OpenJevは単一のモデル名ではない。例えばSGLang実装はQwen3.6-35B-A3Bを使う。[S5] DeBERTa版はより小さい判定専用実装だが、公開カードの対象は英語であり、今回の混在ローマ字区間判定への適合は未確認である。[S6] Jevの型付き出力保証と、言語境界の正答保証を混同しない。[S7]

Windows用IMEはTSFを基盤にする。アプリ内にロードされるDLLにはホストの制約がかかるため、無条件に外部通信や重い推論を実行する前提にしない。[S8]

今回の改訂で追加確認した事実:

MozcはGoogle日本語入力に由来するOSSプロジェクトだが、Googleの公式サポート製品ではない。[S14] 公開文書ではWindowsの既定キー設定はMS-IMEで、設定画面から変更・カスタマイズできる。[S15] キー表では入力中のSpaceがConvert、変換中のSpaceがConvertNext、EnterがCommitであり、入力状態ごとに処理が異なる。[S16]

Windows向け候補表示にはmozc_rendererが用いられる。[S15] MyIMEはMozcとAzooKey/Zenzaiを接続しているため、UI・キー処理を再利用する候補になる。[S1] ただしGoogle日本語入力のシステム辞書・連語データなどとMozcの公開データは異なるため、同じ基盤を使っただけで変換品質まで同一になるとは扱わない。[S17]


## 3. 論理構成

```text
Windows TSF / Mozc入力処理
        |
        v
RawBuffer: 元入力・カーソル・リビジョンを保持
        |
        v
MixedDecoder
  - 日本語ローマ字候補
  - 英語そのまま候補
  - URL/識別子などの保護候補
  - 制限付きタイポ修復候補
        |
        +---- 安価な候補と表示を直ちに更新
        |
        v
AzooKey/Zenzai: 日本語候補の変換・評価
        |
        v
JointRanker: 境界・変換・編集コストを合わせて比較
        |
        v
入力・文脈・操作世代が一致する応答だけ反映
  （候補選択中は自動差替えしない）
```

図は論理モジュールであり、複数の常駐サービスを作る指示ではない。C++/Swiftの既存境界を利用し、Python、WSL、Docker、HTTP推論サーバーを通常利用の必須条件にしない。推論を実行する場所は既存mozc_server等の実装を調べて決め、アプリごとに重みを重複ロードしない。

## 4. 全体を英日二択にしない

`Githubnoripojitoriwokousinnsitekudasai` について、例えば次の複数仮説を保持する。

```text
[Github:原文保持] [no...kudasai:日本語]
[Githubno:原文保持] [ripo...kudasai:日本語]
[全文:原文保持]
```

判定器は候補を絞るが、後段が訂正できない二択を先に確定しない。変換結果の自然さと元入力からの変更量を見て順位を決める。英語スコアとZenzaiの条件付きスコアを無校正のまま直接足さない。

提案する評価特徴:

- 英語らしさ、ローマ字としての妥当性、辞書一致、文脈との一致。
- 日本語変換候補の評価、境界の自然さ、未知語を残す妥当性。
- キーボード編集コスト、不要な補完・表記変更への罰則。
- 明示的に登録された単語・補正拒否などのローカルな嗜好。

初期実装では小さなbeamで上位仮説だけ保持する。候補数2/4/8と編集予算を比較測定し、上限を固定する。文章全体に対する全編集列挙や、候補ごとの無制限LLM呼び出しは禁止する。

保護は「ASCIIだから全部保護」ではない。それでは入力例の全文が保護されてしまう。URL・パス・明示的なコード領域は強く保護し、大文字・辞書・既知識別子は境界仮説の手掛かりとして使う。未知の固有名詞や小文字の製品名もテストする。`Github`を自動で`GitHub`へ改名する必要はない。

## 5. タイポと補完の段階

### 5.1 ローマ字の許容表記

`shi/si`、`chi/ti`、文脈依存の`n/nn`等は、まずローマ字規則として扱う。対応する正規化と生入力の位置対応を同時に保持する。

### 5.2 誤打鍵

隣接キー、重複、脱落、入替を重み付きで扱う。日本語候補区間に限定して読み候補を生成し、編集ゼロの枝を必ず残す。正しく入力された別単語を誤修正しないよう、辞書一致だけで上書きを決めない。

### 5.3 表記上の定型補正

挨拶としての`konnitiwa` → `こんにちは`などを処理する。英語の未知語・ユーザー識別子をこの辞書に吸収しない。

### 5.4 意味を伴う補完

`kiyoiitennkidesune.`から`今日はいい天気ですね。`を選ぶには、読み修復だけでなく欠落語の推定も必要になる。まず上位候補に含められるかを測る。文脈が弱い場合は候補提示にとどめ、確定済み文字列へ遡って強制挿入しない。補完の高信頼閾値は専用評価セットで校正する。

強い補完のために別の汎用LLMを追加するのは初期版の必須要件にしない。必要性を示す失敗例が蓄積してから、小型編集モデルか文節末だけの追加評価を比較する。

## 6. 生入力と位置の契約

以下は新規インターフェース案であり、MyIMEに既に存在するAPI名ではない。

```text
InputSnapshot
  session_id, revision, context_generation, interaction_generation
  composition_state, display_mode, manual_override
  visible_candidate_id, candidate_list_generation
  raw_text, raw_cursor
  left_context, right_context
  field_hints, learning_allowed
  punctuation_completion_enabled

Candidate
  output_text
  spans[{raw_range, reading_range, output_range, kind}]
  edit_operations
  score_features
  source_revision, source_context_generation, source_interaction_generation
  stable_candidate_id
```

raw_rangeは未正規化の入力上の位置、reading_rangeは読み上の位置、output_rangeは出力上の位置とする。UTF-8バイト数、Unicodeスカラー数、Swift文字数、WindowsのUTF-16位置を混ぜない。境界ごとに単位を明記し、変換表を持つ。サロゲートペアや結合文字の途中を切らない。

カーソル変更、選択変更、フォーカス変更でも世代番号を更新する。Spaceによる選択開始、候補移動、文節長変更、Fキーによる文字種指定、Esc等による取消もinteraction_generationを更新する。原文が不変でも、操作前の応答は操作後へ適用しない。テキストが同じでも文脈が違えば古い応答を適用しない。TSFへの書込みは対応するスレッド／編集セッションで行い、推論ワーカーから直接COMオブジェクトを操作しない。非同期編集要求でも失敗・切断を処理する。[S9]

## 7. 入力体験: Google日本語入力相当

### 7.1 既定の体験と意図的な拡張

目的は「いつものIMEの操作を覚え直さず、英日切替なしで入力できる」ことである。以下を新規仕様として実装する。現行Google製品が既にこれらの自動化機能を持つという説明ではない。

英日自動判定、タイポ候補生成、自動変換プレビューは有効とする。確度の高い候補を未確定文字列として表示し、入力中の低信頼な候補は読み／原綴り表示のまま待つ。変換結果を出すために毎回Spaceを必須にはしない。一方、従来どおりSpaceを押してからEnterで確定する入力も、余計な操作なしで利用できる。

表示だけを「従来型の読み表示」にする設定も用意してよい。この場合も英日自動判定とタイポ補正は無効化しない。これは初回に選べる表示設定であり、通常入力で言語モードを切り替える仕組みではない。

不可逆な自動確定はタイマーだけでは行わない。確定済み本文を後からAIが修正しない。Enter確定、既存キーマップに従った次の入力など、ユーザー操作に結び付く確定だけを行う。機密欄や保護領域では別の正規な制約が優先される。

### 7.2 キー操作の契約

初期の基準はMozcのWindows既定MS-IMEキーマップとし、既存のキー設定機構を再利用する。[S15][S16] 次表は既定設定を基準とする設計要件の要約であり、全キー表の代用品ではない。カスタム設定がある場合はその設定を尊重する。

| 状態・操作 | 本IMEの契約 |
|---|---|
| 日本語・混在文字列を入力中の最初のSpace／変換キー | 通常の変換選択状態へ入る。ライブ表示済みでも最初の候補を飛ばさない。候補窓の表示タイミング・選択表示は基準版に合わせる。 |
| 変換選択中のSpace、↓ | 次候補。自動言語判定が途中でこのキーを空白へ変更してはいけない。 |
| 変換選択中のShift+Space、↑ | 前候補。 |
| 未確定文字列があるときのEnter | ユーザーが見ている文字列・候補を確定。同じキーで送信／実行まで行わない。推論完了待ちや非表示候補への差替えをしない。 |
| 未確定文字列がないときのEnter | 通常のキーとしてアプリに渡す。 |
| 変換選択中のEsc／Backspace | 基準キーマップのCancelに従い、読み編集へ戻す。いきなり確定済み本文を消さない。 |
| 読み編集中のBackspace／Delete | 基準キーマップと文字位置対応に従って編集する。タイポの自動再補完で削除文字を復活させない。 |
| 読み編集中のEsc | 基準の取消を維持する。既存の取消操作を常に英字復帰へ横取りしない。 |
| F6／F7／F8／F9／F10 | ひらがな、全角カタカナ、半角化、全角英数、半角英数の操作を維持。対象文節／対象範囲、再押下時の挙動も基準に合わせる。 |
| 変換中の←→／Shift+←→ | 文節移動／文節長の縮小・拡張を維持。手動で設定した境界をAIが勝手に戻さない。 |
| Tab、候補番号、ページ移動 | 基準の候補UI・キーマップの状態別処理を再利用。非入力時のTabを無条件に奪わない。 |
| 英語として表示中の区間でのSpace | 通常の英語入力として空白を1回だけ挿入。原綴り・大小文字を維持する。 |
| 未入力時のSpace／Shift+Space、明示的な空白入力コマンド | 選択中のキーマップ／文字幅設定を維持。日本語の空白を入れる操作をなくさない。 |
| 確定取消、再変換、単語登録 | 基準の機能と操作を維持する。対応アプリで試験し、サロゲートペア等を壊す既知不具合は配布前に修正する。 |

半角／全角や既存のIMEオン・オフ操作は残してよいが、日常の英日混在入力で使うことを前提にしない。手動でIMEオフにしたのにAIが勝手に再有効化することは禁止する。

### 7.3 Spaceの解釈はキーを押した時点で固定

自動言語判定が必要なため、Spaceをすべて同じ意味にはできない。ただし処理はユーザーに見えている入力状態と一致させる。

1. 既にユーザーが変換／候補選択を始めている場合は、キーマップどおり候補操作として処理する。
2. 選択前の入力中で、日本語または混在の変換プレビュー／読みを表示している場合は、変換開始として処理する。
3. 英語の原綴り区間として表示している場合は空白入力として処理する。空白を含めた確定を行う場合は、その入力操作と一体として処理する。
4. あとから到着したAI結果によって、消費済みのSpaceを再解釈・削除・再挿入しない。曖昧さは候補選択や文字種変換で回復する。

`hello world`では空白を保持し、`Githubnoripojitoriwokousinnsitekudasai`では末尾の日本語に対してSpace変換できることを必須回帰とする。文脈なしの`no`のような曖昧語に常時完全な判定を要求しない。

英語の原綴りを未確定として保持している場合はEnterを確定に消費するため、IMEオフの直接英数入力と全場面で完全に同じEnter動作にはならない。安全性と後からの英日判定を優先する意図的な差分であり、黙って送信まで通して見かけの互換性を作らない。直接入力が必要な領域・明示的IMEオフは既存経路を利用する。

### 7.4 自動変換と候補選択の状態を分離

`AUTO_COMPOSING`と`MANUAL_CONVERSION`は設計上の論理状態名であり、上流のAPI名ではない。

- `AUTO_COMPOSING`: 自動候補を作る／必要に応じてプレビューする。日本語として変換された見た目でも、文字キーで入力を継続できる。プレビューを通常の確定待ちConversionと同一に扱い、次の文字で早期確定してはいけない。
- `MANUAL_CONVERSION`: Space等によりユーザーが明示的に開始する。最初に表示する候補集合・選択候補ID・文節境界を固定し、その後は従来の操作に従う。
- `MANUAL_OVERRIDE`: Fキー、文節変更、取消などによるユーザー指定。対象範囲へAI候補を強制的に戻さない。新しい原入力・明示的な再変換など、定義した操作で解除する。

ユーザーが候補選択を開始したら、候補の順位・番号・選択対象を非同期結果で並べ替えない。候補窓を再度開く／新たに入力する等、明確な次の機会に更新する。単に原文リビジョンが同じという理由で結果を適用してはいけない。キャンセル後に同じ文字列を再入力した場合も、操作世代で区別する。

AIプレビューがまだ用意できていない時のSpaceは、既にある辞書候補で直ちに動作する。AIが遅いことを理由に候補窓・Enter・Backspaceを待たせない。ユーザーが見て確定した候補を、遅れて到着した高スコア候補へ置換することは禁止する。

### 7.5 見た目・設定・辞書

候補窓はMozcの既存Windows rendererを優先して再利用する。[S15] 独自の巨大パネル、チャットUI、打鍵ごとの「AI思考中」表示、タイプライター演出、別ウィンドウへの入力を加えない。未確定下線、対象文節、候補選択、候補番号、カーソル近傍の表示、マウス選択を維持する。

Google製品そのものと誤認される名称・ロゴは用いない。外観のピクセル単位の複製ではなく、配置・操作・情報密度・応答の連続性を合わせる。既存のDPI、複数画面、アクセシビリティ経路を維持・検証する。

キー設定、ローマ字テーブル、句読点・空白の設定、ユーザー辞書、学習履歴の管理を削らない。ユーザーが自分のGoogle日本語入力から書き出した辞書のインポートを移行試験に含める。読み・単語・品詞・コメント・重複を検証し、Google製品の内部辞書や学習履歴を丸ごと移せるとは断言しない。

MyIME独自の辞書登録候補やダイアログの挙動が通常の変換操作を崩す場合は、初期設定から外すか改善する。特に候補窓からの登録で未確定入力を失わないようにする。使い慣れた機能を減らして軽量化したことにはしない。

### 7.6 明示的な自動補完の範囲

タイポ補正は候補・未確定表示へ適用する。意味を変えうる助詞補完等は慎重に扱い、意図しない補完の取消を簡単にする。数字・URL・コード・識別子を勝手に修正しない。

句点の未入力追加は既定オフ。`konnitiwa`の既定出力目標は`こんにちは`であり、`konnitiwa.`では日本語句読点設定に従って`こんにちは。`を候補にする。句点追加をオンにした別プロファイルでは初版の目標例も評価する。これはユーザーの例を廃棄するものではなく、文末補完の評価を分離する変更である。

## 8. 非同期処理と障害時

キー処理は同期推論を待たない。表示・Space変換・候補送り・Enter確定の主経路は、即応可能な状態／辞書候補を使う。AI補助の80msという予算を、各キーに許される追加待ち時間と解釈しない。まず安価な表示結果を返し、重い変換は並行して行う。実行待ちは最新要求1件を基本とし、古い入力に対する推論が長く列を作らないようにする。中断不能なバックエンドは実行終了まで待ってもよいが、古い結果は捨てて最新要求へ進む。

モデルを常駐キャッシュし、未変更区間を再計算しない。Zenzaiの推論回数上限1を初期比較点とし、曖昧な文節だけ追加評価を試す。複数仮説で文脈が異なる場合はキャッシュキーも分ける。

モデル未導入・ロード失敗・ワーカー異常を区別する。少なくともキー入力と原文確定を維持する。辞書エンジンが正常なら辞書変換を継続し、エンジン全体の失敗時にも完全な辞書フォールバックがあると仮定しない。クラッシュの連続再起動は抑制する。

CPUを必須の動作経路とし、GPUは任意にする。ただしCPUでの目標遅延達成は測定するまで約束しない。Vulkan/CUDA等の追加バックエンドは、導入負担と実測改善が見合う場合だけ採用する。

## 9. 安全とプライバシー

入力文の外部送信は既定で禁止。クラウド教師や遠隔Jevに実入力を送らない。ユーザー学習は許可された確定／明示選択だけを対象とし、自動候補を正解とみなして自己強化しない。履歴はローカル、上限付き、消去可能にする。診断ログには原文を既定で記録しない。

パスワード欄等でTSFが入力サービスを無効化している場合は処理せず、グローバルキーフックで回避しない。InputScopeだけで機密欄を完全判定できるという設計にしない。Microsoft文書でもIS_PASSWORDはサポートされない値とされ、セキュリティ保証ではない。[S10]

コンテキストはアプリが正規に提供する範囲だけを読み、初期版は短い左右文脈に制限する。アプリ全体・クリップボード・他プロセスを巡回しない。AppContainerの制約を回避するIPC/ACL設定は作らない。[S8]

## 10. 評価計画

### 比較する構成

A: 既存基盤の通常変換。  
B: 全体英日判定を先に固定する案。Jev/OpenJevを使う場合は実装・重み・計算機を明記。  
C: 軽量混在判定 + 複数境界仮説 + Zenzai。  
D: C + 制限付き誤打鍵修復。  
E: D + 専用小型文字モデル、または強い補完。必要性がある場合だけ。

Bはアーキテクチャ比較であり、不適切な英語専用モデルだけを相手にして優位性を主張しない。同一の文脈範囲、候補上限、ハードウェア条件を記録する。

### Google日本語入力との操作互換性の検証

同じPC、Windows版、対象アプリ、JIS／US配列、キー設定、学習状態、文字幅・句読点設定を記録し、インストール済みGoogle日本語入力を基準に同一打鍵列を再生する。基準製品のバージョンを記録し、公開Mozcソースの挙動だけをGoogle製品の実測結果として扱わない。

AIを無効にした基盤の試験、AI有効で日本語だけの試験、英日混在の試験を分ける。通常日本語のSpace回数、候補選択・文節修正、文字種指定、確定・取消の結果を比較する。自動英日判定とライブ表示に必要な差分は別表で管理し、その他の挙動不一致を黙認しない。

最終出力、未確定／確定の区別、キーの消費／アプリへの伝播、フォーカス文節、候補ID、アプリの送信／実行回数を記録する。辞書・変換エンジンが異なるため、候補文字列・候補順位の完全一致をキー互換性の判定基準にはしない。学習用データや通信には実入力を無断で使わない。

### 必須指標

英語／識別子の保持率、混在境界精度、変換Top-1/Top-k、誤打鍵修復率、正しい入力を壊す率、取り消し回数、追加操作数、候補の視覚的な入替頻度、入力から表示までのp50/p95/p99、起動時遅延、アイドルCPU、RAM/VRAM。

最終文字列だけでなく1打鍵ずつ再生する。削除、カーソル移動、文脈変更、空白、確定、取り消し、フォーカス移動を含める。合成タイポだけでなく、同意を得た実際の誤打鍵も別評価にする。原文・固有名詞・入力者で訓練と評価を分離し、同じ原文のタイポ違いが両側に漏れないようにする。

付属JSONLは小さな回帰試験用の種であり、精度を主張できる代表的ベンチマークではない。`desired_candidate`をTop-1強制試験に読み替えない。

### 性能の初期目標（未測定）

「使用感同等」は絶対値だけで判定しない。Google日本語入力に対する入力表示、Space、候補移動、Enterの遅延差を同条件で測る。暫定の開発予算として、主経路のp95は基準版+5ms以内、p99は基準版+10ms以内を目標とする。これは達成済み数値でも、すべてのPCでの保証でもない。入力欠落・二重入力・不意の送信は許容しない。


| 項目 | 提案目標 |
|---|---:|
| キー処理の同期部分 | p95 3ms以下 |
| 最初の表示更新 | 約1フレーム以内を目標 |
| AI補助候補の入力→準備完了／反映可能時の反映 | ウォームp95 80ms以下。キーの待ち時間とは別 |
| 重い補正の入力停止→候補反映 | p95 150ms以下、待機時間込み |

数値は設計予算であり現状の達成値ではない。表示の測定にはメッセージ待ちと描画も含める。低消費電力CPU、通常CPU、RTX4090のCPU/Vulkan等を比較し、GPU負荷がある場合とない場合を分ける。文字数32/128/512、コールド／ウォーム、IME対象アプリを報告する。

### 互換性・必須回帰

メモ帳、ブラウザ入力欄、VS Code、Word、ターミナルを最低限確認する。32/64bitアプリ、DPI変更、RDP、サロゲートペア、学習無効時、IME終了中の非同期応答、機密欄、アプリショートカットも確認する。OSやアプリの特定版での結果を、全Windows対応と呼ばない。

## 11. 研究から取り入れる点

2026-09-02公開の「Low-Latency Spell Correction for Japanese Music Search Queries」は、小型BARTと入力方式に沿ったノイズ生成を組み合わせ、GPUで4ms未満を報告する。一方、音楽検索という限定課題であり、表2の正しい入力の保持率も88.36%なので、そのままIMEに十分な精度とは言えない。[S11]

ここから採用するのは「対象に特化した小型モデル」「現実的な誤打鍵の学習」「正しい入力を壊さない評価」である。論文の速度をWindows/CPU/RTX4090の製品性能として転記しない。元入力を一律正規化する手法も、英語保持が必要な本件へそのまま適用しない。

## 12. 実装順序と完了条件

### 段階1: 基盤とヘッドレス試験

MyIMEとazooKey-Windowsの固定コミットを取得し、ビルド、ライセンス、既知不具合と更新差分を確認する。Mozc由来のキー処理・候補UI・設定・辞書機能を維持できることを採用条件にする。あわせてGoogle日本語入力の基準版の打鍵列試験を準備する。まずraw文字列を入力して候補・境界・編集内容・遅延を出す小さなテスト用CLIを作る。TSFの大規模改造はまだ行わない。

完了条件: 付属試験を機械的に再生でき、未対応ケースを未対応として報告する。ユーザー例をハードコードしただけで学習性能を主張しない。

### 段階2: 混在デコーダと軽い補正

原文枝を残すデコーダ、既存Zenzaiへのアダプタ、位置対応、候補校正を実装する。先に重い専用モデルを学習せず、軽量な比較基準を作る。

完了条件: 英語保持と混在境界を独立に測定できる。効果のない機構を削除できる。

### 段階3: TSF統合

入力・文脈・操作世代が一致する応答のみ反映、候補選択中の固定、原文復帰、Google系のSpace／Enter／文節／文字種操作、IME障害時の入力継続を実装する。

完了条件: キー欠落・二重入力・古い応答の適用・意図しない送信が回帰試験で発生しない。実機測定を添付する。

### 段階4: 必要な知能だけ追加

実際の誤分類から文字単位の言語・境界モデルを学習する。候補として100万〜1000万パラメータ級を比較してよいが、このサイズで目標精度が出ると事前断言しない。強い文法補完も別アブレーションで効果と誤変更率を確認する。

配布前には依存コードだけでなく辞書・重み・学習データの再配布条件を確認し、署名・更新・アンインストール・標準IMEへの復帰も試験する。

## 出典

初版調査: 2026-09-24。今回のGoogle系使用感の改訂ではS1、S14〜S17を再取得／追加確認。初版の研究調査は保持した。

[S1] MyIME README: `https://github.com/unok/myime`  
[S2] MyIME architecture: `https://raw.githubusercontent.com/unok/myime/master/docs/architecture.md`  
[S3] Zenzai upstream documentation: `https://raw.githubusercontent.com/azooKey/AzooKeyKanaKanjiConverter/main/Docs/zenzai.md`  
[S4] zenz-v3.2-small Q5_K_M: `https://huggingface.co/Miwa-Keita/zenz-v3.2-small-gguf/tree/main`  
[S5] OpenJev SGLang implementation: `https://github.com/ekzhang/openjev-sglang`  
[S6] Open-Jev DeBERTa model card: `https://huggingface.co/com-kotobalabs/open-jev-deberta-v3-large`  
[S7] TypeSafe Jev introduction: `https://typesafe.ai/blog/introducing-system-one-models-and-jev`  
[S8] Windows IME requirements: `https://learn.microsoft.com/en-us/windows/apps/develop/input/input-method-editor-requirements`  
[S9] RequestEditSession: `https://learn.microsoft.com/en-us/windows/win32/api/msctf/nf-msctf-itfcontext-requesteditsession`  
[S10] InputScope: `https://learn.microsoft.com/en-us/windows/win32/api/inputscope/ne-inputscope-inputscope`  
[S11] Low-Latency Spell Correction for Japanese Music Search Queries, arXiv:2609.04262v1: `https://arxiv.org/abs/2609.04262`  
[S12] azooKey-Windows: `https://github.com/fkunn1326/azooKey-Windows`  
[S13] 混在入力をLLMに渡す別アプリの参考（OS全体のIMEではない）: `https://github.com/rerofumi/fm-japanese-input`

[S14] Mozc README: `https://github.com/google/mozc`  
[S15] Mozc configurations（Windows既定キー設定、候補renderer）: `https://raw.githubusercontent.com/google/mozc/master/docs/configurations.md`  
[S16] Mozc MS-IME keymap: `https://raw.githubusercontent.com/google/mozc/master/src/data/keymap/ms-ime.tsv`  
[S17] Google日本語入力とMozcの違い: `https://raw.githubusercontent.com/google/mozc/master/docs/about_branding.md`  
