# Yomitsugu 入力・変換の設計

対象: 0.2.9 Preview。更新日: 2026-09-26。

Yomitsuguは、英語を交えたローマ字入力を続けながら、日本語部分を変換するWindows IMEである。本書は入力処理、候補、辞書と学習の設計を説明する。操作方法は[使い方](RELEASE.md)、実測値は[変換品質の記録](docs/conversion-quality.md)を参照。

## 1. 元の入力を確定まで保持する

入力したローマ字は確定するまで保持する。変換結果から元の入力を復元する設計にはしない。候補が変わっても削除・挿入・原文への復帰ができることを優先する。

たとえば `APIwokakuninshitekudasai.` は `API` を保持し、後半を「を確認してください。」へ変換する。英語と日本語の境界が曖昧なら複数の候補を残す。`Github` を `GitHub` に変えるなど、製品名の表記を自動で直すことは目的に含めない。

入力中の変換表示は未確定の文字列である。確定はEnterなどの操作に結び付ける。タイマーによる自動確定や、確定後の本文への自動修正は行わない。

## 2. 入力先と変換処理を分ける

```text
Windowsアプリ
    │ キー入力・TSF編集セッション
    ▼
ime_mixed_tip_v13.dll
    ├─ 元の入力・カーソル・候補の選択状態
    ├─ かなのプレビューと候補ウィンドウ
    └─ 継承ハンドルの専用パイプ
           │ 非同期要求・応答
           ▼
    ime_engine_host.exe
           ├─ ローマ字変換・英日境界・タイポ候補
           ├─ AzooKey辞書 / Zenzai
           ├─ 公開辞書 / ユーザー辞書
           └─ 候補の学習
```

TIPはWindowsのText Services Framework（TSF）でキーと未確定文字列を扱う。候補ウィンドウ、設定画面、通知領域のメニューは本プロジェクトのC++実装である。MozcのUI全体を組み込んだ構成ではない。

Swiftの変換ライブラリーとモデルは専用プロセスで読み込む。TIPへ直接ロードしない。プロセス間通信には起動時に渡したパイプを使う。

AzooKeyとの接続にはmyime由来の変換APIを使う。APIが受け取るひらがなと、生のローマ字の位置は別に管理する。依存コード・モデル・辞書の出典は[THIRD_PARTY.md](THIRD_PARTY.md)を参照。

## 3. 候補の生成と順位

日本語としての読み、英語として保持する区間、URLや識別子として保護する区間を比較する。入力欄の種類、大小文字、既知の英単語、ローマ字としての妥当性を使い分ける。ASCII入力を一律に保護すると、日本語ローマ字も変換できなくなる。

読みをAzooKeyへ渡し、公開辞書の語彙や記号を補う。Zenzaiの評価値と独自の境界・編集スコアは、尺度をそろえずに直接加算しない。異なる候補群での点数の役割を明確にする。

ユーザー辞書の登録語を優先し、その次に確定履歴による順位を反映する。元の英字列を選べる候補も残す。公開辞書を増やした際は、語が候補に入るかに加え、正しい候補が下がっていないかを調べる。

### ローマ字とタイプミス

`shi/si`、`chi/ti`、`n/nn`、`ltu/xtu`などはローマ字規則として処理する。規則表にはMozc由来のデータを使う。長音を含む `insuto-ru` を英語のハイフンとして分断しない。

誤打鍵の補正では、文字の置換・脱落・重複・入替を試す。公開辞書にある読みと照合してから変換器へ渡し、入力長と編集回数で探索量を制限する。`aninsuto-ru` のように「ん」の境界が曖昧な入力では、通常のローマ字解釈と辞書に一致する解釈を比較する。

補正しない候補を必ず残す。URL、パス、コードを通常の文章と同じ基準で補正しない。削除した文字を再び補ってしまう問題も、打鍵列の試験で確認する。

### 語句の補完

タイプミスの修正と、入力されていない語句の補完は別に扱う。句点の自動追加は既定で無効。入力された `.` は文章中では「。」へ変換する。

意味を推測して語や助詞を足す機能は今後の検討対象である。導入する場合は候補提示から始め、正しい入力を変更する割合と取り消し回数を測る。

## 4. 古い応答で手動操作を上書きしない

`DecodeInput`は原文、左右の文脈、入力欄の種類と、次の世代番号を持つ。

| 番号 | 更新する場面 |
| --- | --- |
| `revision` | 文字の追加・削除など、入力が変わったとき |
| `context_generation` | 文脈や入力先が変わったとき |
| `interaction_generation` | 候補選択や文字種指定などの操作時 |

応答を受け取った時点で番号を照合する。入力が同じでも、候補選択やフォーカス移動より前の応答は適用しない。選択中に候補番号や順位を自動で並べ替えない。

TSFへの書き込みは入力先の編集セッションで行い、成功した後に内部状態を進める。失敗した場合は元の状態を保ち、同じキーをアプリへ二重に渡さない。

位置を扱う箇所では単位を明記する。C++の文字列はUTF-8、TSFの文字位置はUTF-16、変換APIの対応長は読みの文字数であり、同じ値として扱えない。文字列を切る際はサロゲートペアやUTF-8の途中を避ける。

## 5. キー操作

Spaceで候補を選び、Enterで確定する操作を基本にする。自動プレビューと、明示的な候補選択を分ける。

| 状態・操作 | 動作 |
| --- | --- |
| 日本語・混在の入力中にSpace | 候補の選択を開始。プレビューがあっても先頭候補を飛ばさない |
| 選択中にSpace / ↓ | 次の候補へ移動 |
| 選択中にShift+Space / ↑ | 前の候補へ移動 |
| 未確定入力があるときにEnter | 候補を確定し、アプリの送信操作へ重ねて渡さない |
| 未確定入力がないときにEnter | アプリへ渡す |
| 候補の選択中にEsc / Backspace | 読みの編集に戻る |
| 読みの編集中にBackspace / Delete | カーソル位置の前 / 後を削除 |
| F6 / F7 / F8 | ひらがな / 全角カタカナ / 半角カタカナへ変換 |
| F9 / F10 | 全角英数 / 半角英数へ変換 |
| 英語として保持した入力のSpace | 英語の空白として扱う |

Spaceの意味は押した時点で決める。後着の候補で空白を取り除いたり、変換操作へ読み替えたりしない。手動でIMEをオフにした後に自動で戻す処理も行わない。

0.2.9では、入力直後のEnterで変換前の文字列を確定しないよう、最新候補が届いていない場合に最大3秒待つ。候補が届けばすぐに確定する。この待機は通常の打鍵には入れない。低速な環境での確定遅延は、今後短縮する対象である。

文節の伸縮、確定後の再変換、キー割当の変更は継続開発の対象。内部に文節移動用の状態があっても、主要IMEと同じ文節編集が完成したとは扱わない。

## 6. 辞書と学習の管理

設定画面ではユーザー辞書の登録・編集・削除、TSVの取り込み、公開辞書の更新、学習の切り替えと履歴削除、アプリの更新を行う。通知領域のメニューとスタートメニューから開ける。

ユーザー辞書はUTF-8のTSVで保存する。取り込み時は形式とサイズを検証し、元の辞書をバックアップしてから置き換える。不正なデータで既存の辞書を消さない。

学習は確定に成功した読みと候補をPC内に記録する。同じ読みでは選択回数を優先し、回数が同じなら新しい選択を優先する。最大2,048件を保存する。原文候補、保護対象の入力、URLなどは記録から除く。

### 公開辞書の更新状態

MozcとEDRDGの公開データをHTTPSで取得し、ローカルの変換辞書を生成する。取得元、出典ファイルと生成結果のSHA-256、語彙数を記録する。ユーザー辞書と公開辞書は別ファイルに保存する。

| 表示・記録 | 意味 |
| --- | --- |
| 最終更新 | 辞書の内容が変わった日時 |
| 最終確認 | 直近の確認を試みた日時 |
| 確認時点で最新版 | 取得に成功し、公開データと内容が一致した状態 |
| 未確認 | 確認記録がない状態。過去の版に日時がなければ「記録なし」と表示 |
| 確認失敗 | 直近の取得・生成・保存で失敗した状態 |

内容が同じなら辞書本体と最終更新日時を維持する。確認に失敗しても、前回成功時の更新日時を失敗日時で置き換えない。日時はUTCで保存し、画面ではPCの時刻設定に合わせる。

### アプリの更新

GitHubのリリース一覧から、新しいWindows x64用インストーラーを探す。利用者がダウンロードを選んだ後、ファイルサイズと公開されたSHA-256を検証して起動する。取得先と転送先は許可したGitHubのHTTPSホストに限定し、通信時間とファイルサイズに上限を設ける。

各版は別フォルダーに配置し、使用中のTIP DLLを上書きしない。更新後に入力アプリを開き直すと新版へ切り替わる。ユーザー辞書と学習履歴は版別フォルダーの外に保存する。

## 7. 入力の保護と障害時の動作

入力文、ユーザー辞書、学習履歴を外部へ送信しない。通信は公開辞書やアプリの更新時に行う。診断では処理回数と成否を記録し、通常の入力内容は記録しない。

TSFが入力サービスを無効化している欄、読み取り専用の欄、保護対象の欄では処理を止める。入力欄の属性だけに依存せず、TSFの状態も確認する。グローバルキーフックで制限を回避しない。

文脈は入力先アプリがTSFを通じて提供する範囲から得る。他アプリやクリップボードを巡回しない。変換プロセスが終了した場合も入力文字列を残し、原文の確定を可能にする。再起動は回数を制限する。

## 8. 遅延の目標

通常の打鍵では軽いプレビューを返し、変換要求を非同期で送る。処理待ちには最新の要求を残し、古い要求を長い列にしない。実行中の推論を中断できない場合も、古い応答は適用せず次へ進む。

CPUを基本の動作経路とする。モデルと候補を再利用し、Zenzaiの推論回数は上限1を基準にする。GPUや追加モデルは、導入の負担に見合う改善があれば採用する。

以下は開発上の目標値であり、測定結果は別途記録する。

| 対象 | 目標 |
| --- | ---: |
| 通常のキー処理の同期部分 | p95 3 ms以下 |
| 最初の表示更新 | 約1フレーム以内 |
| ウォーム時の変換候補の準備 | p95 80 ms以下 |
| 重い補正の入力停止から反映まで | p95 150 ms以下 |

文字数32・128・512、初回と2回目以降、CPU、対象アプリを分けて測る。候補の計算時間と、メッセージ待ち・描画を含む表示遅延を区別する。Enterの待機時間も独立して記録する。

## 9. 評価と改善

最終結果と、打鍵中の状態を両方調べる。単語を一括で渡す試験だけでは、削除、候補選択、素早いEnter、フォーカス移動の不具合を見つけられない。

| 評価対象 | 確認する内容 |
| --- | --- |
| 変換品質 | 先頭と上位候補の正答、英語・識別子の保持、混在境界 |
| タイプミス | 修正できた割合と、正しい入力を壊した割合 |
| 操作 | キーの欠落・二重入力、候補の入替、取り消し、意図しない送信 |
| 永続化 | 辞書・学習の保存、再起動後の反映、設定切替、破損データ |
| 配布物 | 実インストール先での変換、更新、登録解除、データの引継ぎ |
| 性能 | p50 / p95 / p99、初回遅延、CPUとメモリー使用量 |

インストーラーの試験では、ビルド元のランタイムを参照できない状態で変換する。開発ディレクトリに依存した動作を、配布先での成功と取り違えない。

アプリ互換性はメモ帳、ブラウザ、VS Code、Word、ターミナルなどで個別に確認する。OS・アプリの版、キー配列、DPI、学習状態を記録する。単一のRich Editホストでの結果を、すべてのアプリでの結果に広げない。

比較実験では、通常変換、英日境界の判定、タイポ補正、学習を段階的に有効にし、どの変更が効いたかを調べる。訓練用と評価用では原文・固有名詞・入力者を分け、同じ文のタイポ違いが両方に入らないようにする。

研究から取り入れる際は、実際の入力方式に沿った誤りを作り、正しい入力の保持率も測る。検索クエリーなど別の課題で報告された精度や速度を、そのままIMEの性能として使わない。

## 参考資料

- [myime](https://github.com/unok/myime): AzooKeyとの接続とWindows向けランタイム
- [AzooKeyKanaKanjiConverter](https://github.com/azooKey/AzooKeyKanaKanjiConverter): かな漢字変換とZenzai
- [Mozc](https://github.com/google/mozc): ローマ字規則、記号辞書、操作の比較資料
- [Windows IMEの要件](https://learn.microsoft.com/en-us/windows/apps/develop/input/input-method-editor-requirements)
- [TSFの編集セッション](https://learn.microsoft.com/en-us/windows/win32/api/msctf/nf-msctf-itfcontext-requesteditsession)
- [InputScope](https://learn.microsoft.com/en-us/windows/win32/api/inputscope/ne-inputscope-inputscope)

固定リビジョンとライセンスは[THIRD_PARTY.md](THIRD_PARTY.md)、再現手順は[開発ガイド](docs/development.md)を参照。
